#include"main.h"
/**
*add - a function to add two integers and return the result
*@a: first integer input
*@b: second integer input
*Return: Always (success)
*/
int add(int a, int b)
{
return (a + b);
}
